---
description: Audit and complete a reusable Pi harness plus engineering workflow stack
argument-hint: "[scope]"
---

Use `/skill:pi-harness` and `/skill:engineering-workflow` together to bootstrap or audit the Pi engineering stack for:

${1:-this project}

1. Resolve this package's `templates/PROJECT_SETUP.md`; inspect repository evidence and fill its contract without guessing unknown release/production facts.
2. Check Pi resources: `AGENTS.md`, APPEND_SYSTEM, settings, packages, skills, prompts, extensions, themes, conflicts, lifecycle cleanup, and critical TUI visibility.
3. Check the workflow: grill → spec → tickets → implement → review → handoff; discover repository-native validation commands for every ecosystem/workspace.
4. Record monorepo roots, ticket working-directory convention, review-base fallback, scratch/ignore policy, release authority, and optional capability fallbacks.
5. Apply only small reversible setup fixes; ask before architecture/package changes. Keep product identity and deployment facts in the project overlay.
6. Reload and run installed-resource discovery plus one small repository-native workflow smoke.

Return one terse readiness line; expand only unknown decisions, conflicts, unsafe assumptions, and exact files requiring user action.
