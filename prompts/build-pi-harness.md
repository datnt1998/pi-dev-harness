---
description: Design or build a reusable Pi-native agent harness
argument-hint: "<domain-or-goal>"
---

Use `/skill:pi-harness` and design a reusable Pi-native harness for:

$ARGUMENTS

Requirements:
- Audit existing Pi resources first.
- Keep names reusable, not project-specific.
- Target Pi resources: AGENTS.md, .pi/APPEND_SYSTEM.md, .pi/skills, .pi/prompts, .pi/extensions, .pi/themes, and packages.
- Preserve critical Pi visibility: cwd/path, model, context used/max/%, thinking level, token/cost data.
- Propose architecture pattern before editing.
- Ask before large or risky changes.
